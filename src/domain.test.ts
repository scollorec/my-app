import {describe,it,expect} from 'vitest';import {normalisePostcode,isValidPostcode} from './services/postcode';import {distanceMiles,daysSince,filterAndSort,freshness,median} from './domain';import {transformEstablishment} from './services/fsa';import type {Establishment} from './types';
describe('postcode helpers',()=>{it('normalises and validates a postcode',()=>{expect(normalisePostcode(' sw1a1aa ')).toBe('SW1A 1AA');expect(isValidPostcode('SW1A 1AA')).toBe(true);expect(isValidPostcode('nope')).toBe(false)})});describe('domain values',()=>{it('calculates distance and inspection age',()=>{expect(distanceMiles(51.5,-.12,51.5,-.12)).toBe(0);expect(daysSince('2024-01-01',new Date('2024-01-11'))).toBe(10);expect(freshness('2023-01-01',new Date('2026-01-02'))).toBe('old');expect(freshness(undefined)).toBe('unavailable');expect(median([5,1,3,9])).toBe(4)})});it('transforms FSA responses and filters/sorts',()=>{const x=transformEstablishment({FHRSID:2,BusinessName:'A',RatingValue:'5',Latitude:'51',Longitude:'-1',scores:{Hygiene:0}});expect(x.scores?.hygiene).toBe(0);const y:Establishment={...x,id:3,name:'B',rating:'1',distance:2};x.distance=1;const result=filterAndSort([y,x],{radius:5,rating:'',businessType:'',age:'all',sort:'distance'});expect(result[0].name).toBe('A')});
