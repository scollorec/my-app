import {describe,it,expect,vi} from 'vitest';import {geocodePostcode,PostcodeError} from './postcode';import {searchNearby} from './fsa';
describe('external services',()=>{it('reports an unknown postcode',async()=>{const fetcher=vi.fn().mockResolvedValue({status:404,ok:false});await expect(geocodePostcode('SW1A 1AA',fetcher)).rejects.toBeInstanceOf(PostcodeError)});it('sends FSA v2 headers and transforms a response',async()=>{const fetcher=vi.fn().mockResolvedValue({ok:true,json:async()=>({totalCount:1,establishments:[{FHRSID:1,BusinessName:'Cafe'}]})});const result=await searchNearby(51,-1,1,fetcher);expect(result.items[0].name).toBe('Cafe');expect(fetcher.mock.calls[0][1].headers['x-api-version']).toBe('2')})});
