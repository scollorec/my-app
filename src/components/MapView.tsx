import { MapContainer,TileLayer,CircleMarker,useMap } from 'react-leaflet'; import type { Establishment,SearchLocation } from '../types'; import { useEffect } from 'react'; import 'leaflet/dist/leaflet.css';
function Recenter({location}:{location:SearchLocation}){const map=useMap();useEffect(()=>{map.setView([location.latitude,location.longitude],13)},[location,map]);return null}
export function MapView({location,items,selected,onSelect}:{location:SearchLocation;items:Establishment[];selected?:number;onSelect:(id:number)=>void}){return <MapContainer center={[location.latitude,location.longitude]} zoom={13} className="map" aria-label="Map of nearby establishments"><Recenter location={location}/><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{items.filter(x=>x.latitude!==undefined&&x.longitude!==undefined).map(x=><CircleMarker key={x.id} center={[x.latitude!,x.longitude!]} radius={selected===x.id?11:8} pathOptions={{color:selected===x.id?'#062d2b':'#fff',fillColor:x.rating&&Number(x.rating)<=2?'#e28354':'#19766d',fillOpacity:1,weight:2}} eventHandlers={{click:()=>onSelect(x.id)}}><title>{x.name}</title></CircleMarker>)}</MapContainer>}
